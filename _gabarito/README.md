# 📦 _GABARITO (não editar)

Código-fonte ORIGINAL do projeto, arquivado como referência.
Durante a trilha você reconstrói cada peça do zero nas pastas da raiz
(`conf/`, `src/`, `notebooks/`, `tests/`) e compara com o que está aqui.

Não desenvolva aqui dentro — isto é só a "folha de respostas".
